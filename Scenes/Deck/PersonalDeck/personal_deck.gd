extends Control

var cards : Array = []
var face_down : Array = []
var face_up : Array = []
# Called when the node enters the scene tree for the first time.
func _ready():
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
	
func increment_deck():
	print_debug("top card: ")
	
func get_top_card():
	pass
	
#func _input(event):
	#print(event.as_text())
